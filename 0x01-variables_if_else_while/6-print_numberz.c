#include <stdio.h>
/**
 * main - prints numbers between 0 to 9
 * Return: Always 0 (success)
 */
int main(void)
{
	int k;

	for (k = 23; k < 33; k++)
	{
		putchar(k);
	}
	putchar('\n');
	return (0);
}
